#ifndef CALCUL_H
#define CALCUL_H


class Calcul
{
public:

    float inverse(float nombre);
    int quotient(int m, int n);
    int reste(int m, int n);
    float min(float a, float b, float c, float d);
    float max(float a, float b, float c, float d);
};

#endif
